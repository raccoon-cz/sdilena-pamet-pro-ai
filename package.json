{
  "name": "sdilena-pamet-pro-ai",
  "version": "0.1.0",
  "description": "Jedna soukromá paměť pro ChatGPT a Claude. Data zůstávají pouze ve vašem počítači.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm run clean && npm run build:assets && (vite build --config vite.config.sidepanel.ts --watch & vite build --config vite.config.background.ts --watch & vite build --config vite.config.content.ts --watch & wait)",
    "build": "npm run clean && npm run build:sidepanel && npm run build:background && npm run build:content && npm run build:assets",
    "build:sidepanel": "vite build --config vite.config.sidepanel.ts",
    "build:background": "vite build --config vite.config.background.ts",
    "build:content": "vite build --config vite.config.content.ts",
    "build:assets": "node scripts/copy-assets.js",
    "clean": "rm -rf dist",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src tests --ext .ts",
    "icons": "python3 scripts/make_icons.py"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.279",
    "@types/node": "^26.1.1",
    "@typescript-eslint/eslint-plugin": "^8.15.0",
    "@typescript-eslint/parser": "^8.15.0",
    "eslint": "^8.57.1",
    "jsdom": "^24.1.3",
    "typescript": "^5.6.3",
    "vite": "^5.4.11",
    "vitest": "^2.1.5"
  }
}
